---
- hosts: all
  become: yes
  tasks:
    - name: install webserver on ubuntu node
      apt: 
        name: apache2
        state: present
      
    - name: I want to start webserver on ubuntu nodes
      service:
        name: apache2
        state: started